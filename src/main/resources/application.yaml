spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: holyboy191
stripe:
  apiUrl: https://api.stripe.com
  timeout: 1000
  enabled: true
  supported-countries: USD,EUR,GBP

payment-gateway: paypal

mail:
  port: porter
  host: host